{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd Timesheetapp/frontend && npm ci && npm run build && cd ../backend && npm ci && mkdir -p public && cp -r ../frontend/dist/* public/"
    },
    "deploy": {
        "startCommand": "cd Timesheetapp/backend && node src/server.js",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}